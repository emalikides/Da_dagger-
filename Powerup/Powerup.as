package Powerup {	import flash.display.MovieClip;	import flash.geom.Point;	//class that defines objects a player can get to upgrade itself, for example, weapons,	// extra health etc...	public class Powerup extends MovieClip {		//a thing of benefit to the player, could be health or a weapon.		public var thing:Object;		//the  point the user must collide with in order to get this.		public var sigpt:Point;				public function Powerup() {		//TODO		}	}}